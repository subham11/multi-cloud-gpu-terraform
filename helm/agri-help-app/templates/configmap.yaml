apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agri-help-app.fullname" . }}-config
  labels:
    {{- include "agri-help-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
